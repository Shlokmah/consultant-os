import EmptyModule from "@/components/EmptyModule";

export default function TasksPage() {
  return (
    <EmptyModule
      description="Every task, standalone or linked to a project, with owner, due date, priority and status. Coming in Phase 6."
      emptyLabel="No tasks yet."
    />
  );
}
