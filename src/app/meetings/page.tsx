import EmptyModule from "@/components/EmptyModule";

export default function MeetingsPage() {
  return (
    <EmptyModule
      description="Meeting notes, decisions, action items and follow-up dates, linked to a client and project. Coming in Phase 6."
      emptyLabel="No meetings yet."
    />
  );
}
