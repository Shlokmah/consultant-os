import EmptyModule from "@/components/EmptyModule";

export default function ClientsPage() {
  return (
    <EmptyModule
      description="Your client list will live here — add, edit and archive clients, with contacts and notes on each one. Coming in Phase 4."
      emptyLabel="No clients yet."
    />
  );
}
