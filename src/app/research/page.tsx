import EmptyModule from "@/components/EmptyModule";

export default function ResearchPage() {
  return (
    <EmptyModule
      description="Research notes with source, tags and an optional link to a client or project. Coming in Phase 7."
      emptyLabel="No research entries yet."
    />
  );
}
